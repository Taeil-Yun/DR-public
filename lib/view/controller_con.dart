import 'package:flutter/material.dart';

class ScrollControllerModel {
  // main scroll controller start
  ScrollController get cardScrollController => ScrollController();
  ScrollController get headlineScrollController => ScrollController();
  ScrollController get galleryScrollController => ScrollController();
  ScrollController get allListScrollController => ScrollController();
  // main scroll controller end
}